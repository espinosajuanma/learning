# Use `scp` to copy files between hosts using ssh

Use this command to copy a local file to a remote host by ssh.

```bash
scp ~/local/file username@hostname:/target/path
```

- **scp**: Secure File Copy
