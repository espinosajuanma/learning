# `tmux` open, rename and switch windows

- Use combination `Ctrl+B` and `c` to open a new windows
- Use combination `Ctrl+B` and `{Windows_Number}` to switch to N window
- Use combination `Ctrl+B`, then `,` to rename a window
